<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@300&family=Playfair+Display&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header class="container-fluid">
        <div class="row mx-3">
            <h1 class="title col-3 align-self-center text-center"><a href='/'>Tech Blog</a></h1>
            <nav class="col-6 offset-2 d-flex gap-4 justify-content-end align-items-center">
                {{#if logged_in}}
                <li class="d-inline"><a href='/'>Home</a></li>
                <li class="d-inline"><a href='/api/dashboard'>Dashboard</a></li>
                <button type="button" class="btn btn-dark" id="logout-btn">Logout</button>
                {{else}}
                <li class="d-inline"><a href='/login'>Login</a></li>
                {{/if}}
            </nav>
        </div>
    </header>
    {{{body}}}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>
</html>